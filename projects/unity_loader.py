# unity_loader.py
# One job: knows the Unity project folder layout and reads the right files from disk.
# Given a root path, it finds and returns all relevant Unity files
# (scripts, scenes, prefabs, assets, settings) as structured content
# that GameProject can store and agents can query against.

pass
