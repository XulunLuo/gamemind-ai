# game_project.py
# All project management in one place.
# Defines what a GameProject is (name, engine, isolated file context),
# and handles registering, retrieving, and switching between projects at runtime.

pass
